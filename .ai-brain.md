# misplacedambience — .ai-brain.md

## Status
Empty, buildable Gradle skeleton only (Package 3, disaster recovery — see
`B:\Claude\RECOVERY.md`). No source copied in yet. `./gradlew build` verified
clean (0 source files, jar produced and confirmed present).

## ★ Reconstruction base: use the 1.20.1 decompile, NOT the 1.21.1 jar

Copy in from:
`B:\Claude\Projekte\_RECOVERY\decompiled\misplacedambience-forge-1.20.1\`

**not** from `misplacedambience-neoforge-1.21.1\`. The 1.20.1 build has 15
sound sources; the published 1.21.1 jar has only 10, missing bell
resonance, minecart rumble, armor stand creak, water drip, and distant
mining — all documented in RECOVERY.md section 5 as existing features. Full
detail in the 1.20.1 folder's `RESTORE_NOTES.md`.

**Bonus resolved from that same source:** RECOVERY.md records Noah saying
"keine Ahnung wie Anker" about the distant-mining feature's anchor point.
Resolved: the anchor is simply the player's vanilla respawn position
(`player.getRespawnPosition()`/`getRespawnDimension()`) — no separate
"set anchor" mechanic exists or needs building. Worth adding a one-line
in-game clarification (e.g. a tooltip) rather than new infrastructure.

**Sequencing:** copy the 1.20.1 source in first, then port the API surface
to 1.21.1/NeoForge (moddev 2.0.141, NeoForge 21.1.234) afterward — see
RECOVERY.md section 8's API-difference table.

## Not yet done
- No source copied in.
- No `deploy-combined-test-mods.ps1` yet (tracked at the `HorrorTricks/`
  family level).
